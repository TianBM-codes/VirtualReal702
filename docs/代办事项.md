## 代办事项

- [x] 包含模型和结果的op2、只包含结果的op2，给焦璐测试
- [x] match_modal_modes这个函数需要按照郑的新格式
- [x] 模态匹配建立一个新的项目走通
- [x] 计算传入的文件的接口：solver/run_and_parse，传入的是文件名，我需要根据projects_id还有环境变量拼接出这个文件的具体位置。这个逻辑和解析完op2然后调用接口入库时的拼接逻辑是一样的，先别修改程序。
- [ ] 我现在想做计算完频率灵敏度后生成对应的h5后处理文件。你先去查看一下程序中bayesian算法生成的结果是如何追加到src文件夹中的整个后处理系统中的，我印象中是有对应的方法，请参考docs/l3/L3-API-Quick-Reference.md文件。先不要修改程序。
- [x] 灵敏度结果是否正确
- [ ] 参数更改量
- [x] 新增频率匹配散点图功能，返回的格式仿照我之前的返回图表折线的格式，横坐标是计算的模态频率值，纵坐标是试验读取的频率值，然后散点是匹配的模态对。先不要修改程序。
- [x] 仿照src文件夹下的log接口，我希望将我的功能也输出到log中，你先找到方案，然后在修改程序。
- [x] app.py去掉过时提醒。
- [ ] 静力的inp现在是否能自动生成。
- [ ] 模态计算的参数和响应定义。
- [x] 存储模态结果到数据库。
- [x] t_static_test_data表格内容变了。
- [ ] t_mt_py_fem_supported_quantity表内容过多。

## 0524 ##

- [ ] 操作文档，静力部分。
- [x] 焦璐的是否可以直接在界面中查看结果？
- [x] pbs给出接口、直接计算也给出接口。
- [ ] 平南特大桥挠度布置。
- [x] pbs写入外网，然后导入后再测试文件是否可以下载。
- [x] pbs计算得到odb后的处理方式和调用本地计算后的处理方式相同。我有两个地方调用了外部的程序计算inp，然后获取odb，最后上传odb到系统。但是现在调用本地的abaqus得到的odb可以上传到系统中，但是调用pbs计算下载到的odb无法上传到系统中，请你查看问题可能出现在哪里了，然后和我讨论，先不要修改程序。
- [ ] 还需要功能描述。
- [x] 在解析abaqus的时候，会读取inp文件中的set，然后存入到数据库中。针对nastran的bdf，我希望用property来做set存入数据库。
- [ ] 相位是否一致，-1是否起效果了？
- [x] 李洁程序。
- [ ] inp生成dsa的程序还得测试多个part的是否可行。
- [x] abaqus的inp灵敏度计算的修改会涉及到后续的模型修正吗？
- [ ] service_config中的file_list接口要去掉file。
- [x] 同事发现的多次读取配置文件的问题，我应该删去。但是我忘了是否修改过了，请你看一下我最近10天左右的git提交记录，看是否有修改。
- [x] 我现在完成了bdf的以频率为响应的灵敏度求解，我现在想将模型修正也融入进去。涉及到响应的选择，那么就是频率，参数的选择，就是密度、弹性模量。模型修正的目标是将模态匹配后的频率作为目标。后续的都要和静力的模型修正类似，也要入库，静力的接口实现的内容，这次频率修正也要实现。先别修改程序
- [x] 对于abaqus的灵敏度分析，有一个功能是解析所有的abaqus的分析结果，然后最后整合到一块，然后再生成到对应的h5文件中，请先找到相关的调用逻辑。我现在的nastran灵敏度求解后，我解析完结果，也想生成同样的h5文件到文件夹中，你先研究一下调用逻辑和实现方式。不要修改程序。
- [ ] 动力修正应该去除刚体模态，比如传入的bdf文件中是无约束的，那么我第一次计算bdf的时候会生成sol103文件，用这个文件去求解模态信息，也就是说按照限制生成bdf_sol103文件，但是这个限制现在还没有相应的配置的地方。

## **0608**

- [ ] 对于Nastran灵敏度分析而言，各个单元属于不同的属性，如果我想对某个set的单元的弹性模量设置为一个参数呢，也就是如何让这个set的单元共享一个弹性模量？你可以参照sol200_sensitivity_vtu_standalone.py脚本理解一下。我参考了MSC Nastran Quick Reference Guide文档，了解了几个字段的含义。你还是向我介绍一下程序里的逻辑吧。
- [x] 我现在想做一个简化版本的，就是系统在解析完bdf后，系统会将这个模型有哪些变量可以作为参数整理出来，以及其作用域，写入到数据库t_mt_py_fem_quantity_set_capability中。对于我上传的bdf文件来说，里面定义了四个材料，也就是4个MAT，但是t_mt_py_fem_quantity_set_capability表中包含E的项目有18个。property确实有16个，因为某个属性不包含任何单元。
- [ ] 接口/optimization/sol200/parameter/create和/optimization/parameter/create两者有什么区别？是否可以取消后者，后者在哪里进行调用了？
- [ ] 对于灵敏度分析而言，我只对某一个set的弹性模量作为参数，那么在我计算得到结果后，显示的时候，只有这个set有值，其他单元没有值，也就是云图应该是有些单元有颜色，有些没有。

## **0609**

- [ ] 前端显示部分是灰色

- [ ] abaqus多个part的inp文件生成

- [ ] 复模态计算MAC值测试

- [ ] bdf测试还未测试弹性模量、密度同时计算灵敏度，属性中的E还未测试

- [ ] 我现在导入了一个inp文件，然后在表格t_mt_py_fem_quantity_set_capability中，只有set_scope为ASSEMBLY的时候，instance_name才包含值，对于set_scope是PART的时候，instance_name为空。这对于我后续创建参数有什么影响？

- [ ] 找到标记模态匹配的标记，给邵，然后他返回给付

- [ ] 求解参数和相应在界面中有设置，但是我暂时还没有用

- [ ] bdf文件中，如果是选择property的弹性模量，那么应该新建一个弹性模量对应单元，否则多个property会重复对应一个弹性模量。

- [ ] 重新计算问题，就是一些已经匹配的匹配对会消失，要支持这种情况。

- [ ] 现在读取结果的部分逻辑有点混乱，也就是我在存储灵敏度结果和模型修正的时候，然后调用接口api/odb/254/results/frame-scalar-range然后会出问题，然后也要观察接口

- ```
  @router.get("/sensitivity/step_frame")
  async def step():
      return ok([{"label": "Sensitivity", "value": "Sensitivity", "frame": 0}])
  ```

  ```
  @router.get("/model-update/step_frame")
  async def step():
      return ok([{"label": "Bayesian-Update", "value": "Bayesian-Update", "frame": 0}])
  ```

  这是两个我用来返回给前端的默认的step，然后api/odb/254/results/frame-scalar-range中的参数也需要考虑一下。

- [ ] Nastran的PBS还未调通

- [ ] 对于Abaqus的参数创建部分，是不是part_name、set_name、quantity_code为必须指定的，然后就能唯一确定参数了？和Nastran的bdf导入后的创建参数有什么异同。我创建这些参数主要是用来后续的灵敏度计算的。我认为这需要比较复杂的逻辑，需要知道某个参数是如何能够锁定的。还有选出这些参数就是为了后续生成求解灵敏度的inp文件的。所以这是一整套逻辑。

- [ ] `POST /import/fem/static/from_project_result`这个接口现在有点慢，他的作用是什么？是不是不需要一定要存入数据库？现在先改成异步，然后step参数改为最后一个step，而不是默认的"Step-1"

## **0616**

- [ ] 现在Odb文件过大的时候调用import/fem/static/from_project_result接口会超时，考虑将写入数据库做成异步的，或者用到具体数值的时候再去读取。
- [ ] 门导入后，几何属性没有梁单元和壳单元数据显示
- [ ] 几何属性对各向异性的支持情况
- [ ] 灵敏度计算结果和参数更改量结果需要支持按照set显示，也就是说需要用set过滤一下，只显示渲染选中set的单元，选中set的最小值是蓝色，最大值是红色。
- [ ] 动力修正，以频率为响应，添加了两个响应，但是在响应跟踪的图形中，只有一组结果。