# 小白版：有限元、ODB、模型显示和 Three.js 是怎么串起来的

## 这份文档是干什么的

这不是实现文档，也不是 Abaqus 教程。

这份文档只做一件事：

**用大白话解释这个项目里“有限元结果”和“前端模型显示”到底是什么关系。**

如果你现在的感觉是：

- 我知道需求大概是什么
- 但我不懂 ODB
- 也不懂有限元结果到底怎么变成前端画面

那这份文档就是给你的。

---

## 1. 先说最核心的一句话

这个项目本质上是在做两件事：

1. 把 Abaqus 的仿真结果文件 `ODB` 读出来
2. 把里面的“分析数据”变成前端 Three.js 能显示和交互的东西

所以它不是一个普通的“3D 模型查看器”，而是一个：

**有限元结果查看器**

---

## 2. 有限元到底是什么

可以先把它粗暴理解成：

工程师把一个真实物体，拆成很多很多小块，交给计算机去算受力、变形、应力这些东西。

这些“小块”不是为了好看，而是为了计算。

有限元模型里最重要的两个东西是：

- **节点（Node）**
- **单元（Element）**

### 2.1 节点是什么

节点就是空间中的点。

比如一个零件表面和内部，会布满很多计算点，这些点就是节点。

节点通常有：

- 编号
- 坐标

### 2.2 单元是什么

单元就是由若干节点拼起来的一个小计算块。

比如：

- 三角形壳单元
- 四边形壳单元
- 四面体实体单元
- 六面体实体单元

你可以把它理解成：

- 节点是骨架上的点
- 单元是用这些点连起来形成的小片、小块

计算真正很多时候是发生在单元上的，不一定直接发生在“前端看到的三角面片”上。

---

## 3. ODB 是什么

ODB 是 Abaqus 的结果文件。

你可以把它理解成一个大数据库，里面装着：

- 模型的节点
- 模型的单元
- 零件实例
- 材料
- 截面
- 集合
- 每一步分析的结果
- 每一帧的位移、应力、应变等数值

所以 ODB 不是“前端模型文件”，它更像：

**仿真模型 + 仿真结果 + 组织结构**

---

## 4. 为什么 ODB 不能直接给 Three.js 画

因为 ODB 里的数据是给仿真后处理软件用的，不是给浏览器渲染器直接用的。

前端 Three.js 擅长的是：

- 顶点
- 三角面
- 法向
- 颜色
- 材质

但是 ODB 里原生更多是：

- 节点
- 单元
- 结果场
- 积分点
- 截面点

这两套东西不是一回事。

所以中间必须有一层转换：

**把有限元数据变成适合前端渲染的数据。**

---

## 5. 前端显示的“模型”到底是什么

你现在可以把前端显示的模型理解成：

**一个为了显示而生成的表面壳子。**

这个壳子不是 ODB 原始模型本体，而是从有限元模型里提炼出来的“可视化版本”。

在你们这个项目里，这个壳子主要是：

- 表面三角面片
- 平滑法向
- 面片编号
- 面片和原始单元/节点的映射关系

这就是为什么你同事会说“前端只有三角面片”。

因为 Three.js 最擅长画这个。

---

## 6. 为什么最后都变成三角面片

因为图形渲染里，三角形最稳定、最通用。

不管原始单元是：

- 四边形
- 六面体
- 四面体
- 高阶壳单元

最后为了前端显示，通常都会想办法转成一堆三角面。

你可以把它理解成：

- 有限元世界里的对象，适合计算
- Three.js 世界里的对象，适合渲染

而“三角面片”就是这两个世界之间最常见的桥梁。

---

## 7. 云图是什么

云图本质上就是：

**把某个数值结果映射成颜色。**

比如：

- 蓝色表示值小
- 红色表示值大

常见云图内容有：

- 位移云图
- 应力云图
- 应变云图

也就是说，云图不是另一套模型，而是：

- 模型还是那个模型
- 只是每个位置带了一个结果值
- 再把这个值映射成颜色

---

## 8. 变形显示是什么

变形显示也不是另一套模型。

它本质上是：

- 原始坐标
- 加上位移结果

也就是：

`变形后位置 = 原位置 + 位移 * 缩放因子`

这里的位移通常来自 ODB 里的 `U` 结果。

所以当你看到模型“动起来”或者“鼓起来”“弯下去”，本质上是：

- 顶点位置被改了
- 不是重新换了一个完全不同的模型文件

---

## 9. step、frame、field 是什么

这是 ODB 里很重要的一组概念。

### 9.1 step

可以先理解成：

一次分析里的一个阶段。

比如：

- 静力分析一步
- 模态分析一步
- 动力分析一步

### 9.2 frame

frame 就是某个 step 里的某一帧结果。

比如：

- 某个时间点
- 某个模态阶次
- 某个载荷阶段

### 9.3 field

field 就是“结果场”。

比如：

- `U`：位移
- `S`：应力
- `E`：应变

你可以把它们连起来理解成：

- `step` 决定分析阶段
- `frame` 决定看哪一帧
- `field` 决定看什么物理量

前端切换云图时，很多时候本质上就是在切：

- 当前 `step`
- 当前 `frame`
- 当前 `field`

---

## 10. NODAL、INTEGRATION_POINT 是什么

这是理解“结果怎么显示”的关键。

### 10.1 NODAL

意思是结果值挂在节点上。

这类结果很适合做：

- 平滑云图
- 变形显示

因为前端显示时，一个三角面正好有 3 个顶点，顶点拿节点值就能插值出平滑颜色。

### 10.2 INTEGRATION_POINT

意思是结果值挂在单元内部的积分点上。

这是很多应力结果真正原始的计算位置。

这类结果更适合做：

- 单元均色
- 单元结果查询

如果想把它显示得很平滑，就需要额外规则去外推、平均或转换。

### 10.3 为什么这件事重要

因为不是所有结果都天然适合前端直接拿来画。

有些结果：

- 适合按节点显示
- 有些适合按单元显示
- 有些需要工程规则决定怎么显示

所以你们后端不能只做“文件读取”，还要做“结果映射”。

---

## 11. instance 是什么

可以先把 `instance` 理解成：

**装配里的一个零件实例。**

比如同一个零件模板，装了两份到总装模型里，就可能有两个 instance。

这件事重要是因为：

- 节点编号不一定全局唯一
- 单元编号也不一定全局唯一

很多编号只在各自 instance 内部唯一。

所以系统里很多查询不能只说：

- 节点 123

而要说：

- `instance = A` 里的节点 123

---

## 12. 为什么需要 L1、L2、L3

这是你们项目最重要的架构分层。

### 12.1 L1 是什么

L1 做的事是：

**把 ODB 原始内容尽量忠实地转出来。**

它像“原始数据层”。

里面重点是：

- 节点
- 单元
- 集合
- 材料
- 截面
- 各种结果数据

L1 不负责前端显示友好。

### 12.2 L2 是什么

L2 做的事是：

**把 L1 里的有限元数据加工成适合前端显示和交互的数据。**

比如：

- 表面提取
- 三角化
- 生成显示壳子
- 生成法向
- 建立面片到原始单元/节点的映射

L2 像“渲染预处理层”。

### 12.3 L3 是什么

L3 做的事是：

**根据前端请求，把 L1 和 L2 组合起来，快速返回可显示、可查询的数据。**

比如：

- 给前端一块 mesh chunk
- 查询某一帧某个字段的云图结果
- 点击某个面后，返回对应单元的详细信息
- 生成变形后的坐标

L3 像“运行时查询和组装层”。

---

## 13. render_face_idx 是什么

这是你们这个系统里一个很关键的概念。

你可以把它理解成：

**前端显示壳子里，每个三角面片的身份证号。**

前端点击某个三角面，其实点到的就是某个 `render_face_idx`。

然后后端通过这个编号去查：

- 它来自哪个单元
- 它关联哪些节点
- 它当前的结果值是什么
- 它属于什么材料、截面、集合

所以这个编号是：

**前端显示世界 -> 有限元语义世界**

之间最重要的桥梁之一。

---

## 14. Three.js 在这里到底负责什么

Three.js 在这个项目里不是用来“理解有限元”的。

它只是负责：

- 把后端给的三角面片画出来
- 接受用户交互
- 进行相机控制
- 显示颜色、变形、选中状态

它不应该负责：

- 解析 ODB
- 理解积分点结果
- 自己从体单元生成表面壳子
- 自己决定有限元结果如何映射

所以 Three.js 在这里更像：

**显示壳子 + 交互壳子**

不是“分析引擎”。

---

## 15. 前端和后端各自最合适干什么

### 15.1 后端更适合做的事

- 解析 ODB
- 管理大数组
- 结果映射
- 变形计算
- 集合过滤
- 点击命中后的语义回查

### 15.2 前端更适合做的事

- 画面显示
- 相机控制
- 交互事件采集
- UI 状态切换

一句话说：

- 后端负责“算”
- 前端负责“画”

---

## 16. 你可以怎样理解整个系统

可以把整个系统粗暴理解成三句话：

### 16.1 第一句

ODB 里存的是“工程分析世界”的数据。

### 16.2 第二句

Three.js 里画的是“浏览器显示世界”的数据。

### 16.3 第三句

L1/L2/L3 做的事，就是把这两个世界接起来。

---

## 17. Step、Frame、U、USUM 是什么

### 17.1 Step（分析步）

Abaqus 把一次完整仿真拆成多个 **Step**，每个 Step 代表一个独立的加载阶段。

```
Step-1: 施加重力        （静力分析）
Step-2: 施加侧向力      （接着上一步继续）
Step-3: 释放约束        （另一种工况）
```

不同 Step 代表不同的物理场景，结果不能混在一起看。

---

### 17.2 Frame（帧）

每个 Step 里有若干 **Frame**，相当于这个加载过程中的采样时刻。

```
Step-1, Frame 0: 载荷 =   0%，结果快照
Step-1, Frame 1: 载荷 =  25%，结果快照
Step-1, Frame 2: 载荷 =  50%，结果快照
Step-1, Frame 3: 载荷 = 100%，结果快照
```

前端"拖动时间轴"就是在切换 Frame。每帧都有独立的结果数据。

---

### 17.3 U — 位移场（Displacement）

结构受力后每个节点**偏移了多少**，分三个方向分量存储：

| 分量 | 含义 |
|---|---|
| **U1 / Ux** | X 方向位移（单位通常是 mm 或 m）|
| **U2 / Uy** | Y 方向位移 |
| **U3 / Uz** | Z 方向位移 |
| **USUM** | 总位移幅值 = √(Ux² + Uy² + Uz²) |

直觉理解：一根梁弯曲后，每个节点偏移了多少、往哪个方向偏。USUM 就是"偏了多远"，不关心方向，所以恒 ≥ 0。

在代码里，U 存成 `[N, 3] float32`，每行是一个节点的 `[Ux, Uy, Uz]`，USUM 用的时候算：

```python
usum = np.linalg.norm(data, axis=1)  # [N]
```

---

### 17.4 其他常见结果场

| 字段 | 全称 | 典型用途 |
|---|---|---|
| **S** | Stress，应力 | 判断会不会断裂 |
| **E** | Strain，应变 | 判断变形程度 |
| **RF** | Reaction Force，约束反力 | 查支撑点受力 |
| **MISES** | Von Mises 等效应力 | 最常用的强度判据，标量 |

---

### 17.5 它们在 L1 里长什么样

```
l1_raw/results/Step-1_U/PART-1-1/NODAL/
    labels.npy      # 节点 label 数组 [N]
    f0000.npy       # Frame 0，[N, 3] → 每节点 [Ux, Uy, Uz]
    f0001.npy       # Frame 1
    f0002.npy       # Frame 2
    f0003.npy       # Frame 3
```

L3 按需读取某一帧：`ds[frame_idx]`，不需要加载所有帧。

---

## 18. 你现在最应该先搞懂的 5 个词

如果你现在不想看太多，先记住下面 5 个词就行：

- `node`：节点，空间中的点
- `element`：单元，由节点组成的小计算单元
- `field`：结果场，比如位移、应力
- `instance`：装配中的一个零件实例
- `render_face_idx`：前端显示三角面的编号

把这 5 个词搞懂，你对整个系统的理解就已经有基础了。

---

## 18. 最后一句人话总结

这个项目不是“把 ODB 文件丢给 Three.js 直接显示”。

而是：

- 先从 ODB 里拿出有限元模型和结果
- 再加工成前端能画的表面三角壳子
- 再把结果值、变形、点击查询这些能力挂到这个壳子上
- 最后由 Three.js 负责显示给用户看

所以真正难的不是“画三角形”，而是：

**怎么把有限元结果正确地翻译成可显示、可交互的前端数据。**

---

## 19. 云图颜色是怎么算出来的

### 19.1 colormap 是什么

colormap（色图）就是一张"数值 → 颜色"的对照表。

最常见的是 **jet**：蓝色代表小值，红色代表大值，中间经过绿、黄过渡。

```
0.0 → 蓝色
0.5 → 绿色
1.0 → 红色
```

但 colormap 只认 0~1 之间的值，所以原始物理量（比如位移 0.15m）要先变成 0~1，这一步叫**归一化**。

---

### 19.2 归一化是什么

```
归一化值 = (当前值 - 最小值) / (最大值 - 最小值)
```

举例：
```
这一帧位移范围是 0m ~ 0.2m
某节点 USUM = 0.15m
归一化 = (0.15 - 0) / (0.2 - 0) = 0.75
→ 查 jet 75% 位置 → 橙色
```

这就是你看到云图颜色的来源。

---

### 19.3 为什么后端算颜色比前端算更好

前端理论上也能算，但有个问题：

```
10M 节点的模型
→ 前端要先拿到所有节点的原始 float 值
→ 然后在 JS 里逐个算归一化 + 查颜色
→ JS 是单线程，算完可能要好几秒
→ 用户电脑差的话直接卡死
```

后端算完直接发 **uint8 RGB 颜色值**（每个颜色只占 3 个字节）：

```
前端收到的是：每个顶点已经算好的颜色
→ 直接塞进 BufferGeometry 渲染
→ 前端零计算，GPU 直接画
```

---

### 19.4 精度够吗

uint8 只有 256 级，看起来很少，但：

```
位移范围 0 ~ 0.2m
uint8 精度 = 0.2 / 256 ≈ 0.78mm
```

对**云图颜色显示**完全够，肉眼分不出这个差别。

如果用户需要精确数值（比如点击某个面查具体位移量），走 **pick 接口**返回原始 float，不走颜色这条路。

---

### 19.5 DataTexture 是什么

Three.js 里上传 GPU 数据有两种方式：

| 方式 | 用途 |
|---|---|
| 普通图片纹理（ImageTexture）| 贴材质图、贴花纹 |
| DataTexture | 把任意数组上传 GPU，shader 里当数据用 |

这里用的是 DataTexture——不是贴一张图，而是把颜色数组上传 GPU，让 shader 直接用这些颜色画模型。

前端代码大概长这样：

```javascript
const colors = new Uint8Array(buffer);   // 从后端拿到的颜色数据
geometry.setAttribute('color',
  new THREE.BufferAttribute(colors, 4, true)  // 4 = RGBA
);
material.vertexColors = true;  // 告诉 Three.js 用顶点颜色
```


---

## 20. 复模态：实部、虚部、幅值是什么，代码里怎么算

### 什么是复模态？

普通的模态分析（无阻尼系统）里，每个节点的位移是一个**实数**——在某个方向振动，有正有负，很直观。

但如果系统有**阻尼**（比如加了减振器、材料本身有耗散），模态分析的结果就变成**复数**，每个节点的位移有实部和虚部两个分量。

实部和虚部代表什么？可以这样理解：

- 实部 → 这个节点在**某个时刻**（θ=0）的位移快照
- 虚部 → 这个节点在**另一个时刻**（θ=90°）的位移快照
- 两者合起来描述的是：这个节点随相位变化时走出的**轨迹形状**

---

### 节点的运动轨迹是个椭圆

实模态：每个节点在一条**直线**上来回振动。

复模态：每个节点在**椭圆轨迹**上转圈运动（实部和虚部是椭圆的两个轴方向）。

```
实模态（无阻尼）：  •——————•——————•   直线来回
复模态（有阻尼）：  •  →  •  →  •   椭圆轨迹
                   ↑              ↓
```

---

### 各分量的幅值怎么算？

以 X 方向为例，节点 j 的 X 向位移随相位 θ 变化：

```
x(θ) = real_x · cos(θ) - imag_x · sin(θ)
```

这是一个正弦信号，**最大值**等于它的幅值：

```
ux[j] = √(real_x² + imag_x²)   ← 精确
```

代码里的写法完全正确，这就是复数取模的公式：`|a + bi| = √(a² + b²)`。

---

### 合位移 usum 怎么算？

代码里：

```javascript
usum[j] = √(ux² + uy² + uz²)
        = √( (rx²+ix²) + (ry²+iy²) + (rz²+iz²) )
```

这个量在数学上叫做**复向量的 Hermitian 范数**，是最常用的"总振幅"度量。

它有轻微高估：因为 X、Y、Z 三个方向不一定同时达到最大值，但这里直接把三个方向的最大值平方相加——相当于假设它们同时达到最大。

**精确的真实最大 3D 位移**（椭圆长半轴）需要：

```
a² = (|r|² + |i|²)/2 + √( ((|r|²-|i|²)/2)² + (r·i)² )
true_usum[j] = √a

其中：
  |r|² = rx² + ry² + rz²
  |i|² = ix² + iy² + iz²
  r·i  = rx·ix + ry·iy + rz·iz  （实部和虚部向量的点积）
```

**高估了多少？** 最坏情况（实部和虚部垂直且等长）高估 **41%**；实部虚部方向一致时两者完全相等。

---

### 实际工程里用哪个？

| 公式 | 准确性 | 场景 |
|---|---|---|
| `√(ux²+uy²+uz²)`（Hermitian 范数）| 保守高估 ≤41% | 可视化标准用法 ✓ |
| 椭圆长半轴公式 | 精确 | 定量分析、论文 |

可视化只需要**相对比较**（哪里亮哪里暗），偏高估不影响结论，用 Hermitian 范数是工程标准做法。

---

### newPos：变形显示用实部，不用幅值

```javascript
newPos[i] = position[i] + real[i] * scaleFactor
```

这里用的是**实部**，不是幅值。

原因：实部是某个时间截面的真实位移形状，把它乘以放大系数后加到坐标上，显示的是"此刻模型长什么样"。幅值没有方向，没办法用来移动坐标。

---

### scaleFactor 怎么来的？

```javascript
scaleFactor = maxScalarSize / coefficient / max(√(real[i]² + imag[i]²))
```

- `max(√(real[i]²+imag[i]²))` — 整个模型里单个位移分量的最大幅值
- `maxScalarSize` — 模型特征尺寸（比如包围盒对角线长度，由前端传入）
- `coefficient` — 缩放系数（默认 5，可调）

公式的意思：把最大振动幅度压缩到模型尺寸的 1/5（可调），让变形在视觉上合适，不会"炸开"也不会看不见。

---

## 21. 结果场的三种类型：标量、向量、张量

ODB 里的结果场按"有没有方向分量"分三类：

| 类型 | 例子 | 分量 | 说明 |
|------|------|------|------|
| **标量** | STH（壳厚度）、TEMP（温度） | 无，`components=[]` | 每个位置只有一个数值，没有方向之分 |
| **向量** | U（位移）、UR（转角） | U1, U2, U3 | 每个位置有 X/Y/Z 三个方向的值 |
| **张量** | S（应力）、E（应变） | S11, S22, S33, S12… | 每个位置有多个分量描述各方向的组合状态 |

**标量字段的 `components=[]` 是正常的**，不是提取失败。数据实际存在，只是不需要分量名。

---

## 23. 特殊单元类型：没有几何形状的单元

ODB 里除了普通的壳单元、实体单元，还有一些特殊单元。它们在 L1 提取时会被跳过，原因是它们没有可渲染的表面。

| 类型 | 是什么 | 有几何形状吗 |
|------|--------|------------|
| `MASS` | 质量单元，给某个节点附加一个集中质量 | 没有，就是一个点 |
| `ROTARYI` | 转动惯量单元，给节点附加转动惯量 | 没有，就是一个点 |
| `CONN3D2` | 连接单元，连接两个节点，模拟铰链/弹簧/焊点等 | 一条线，两个节点 |
| `IDCOUP3D` | 运动耦合单元，把一个节点的运动传递给一组节点 | 没有实体形状 |

这些单元在仿真计算中是有意义的（比如 `MASS` 影响模态频率，`CONN3D2` 模拟连接行为），但对于**表面可视化**这条路完全用不上——没有面就无法三角化、无法渲染。

所以 L1 遇到这些类型时打印 WARNING 跳过，是正确行为，不是 bug。

如果将来需要查询连接关系或质量属性，才需要单独处理这些单元。

